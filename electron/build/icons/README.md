# Desktop Icons

Place production Electron icons here before release:

- `icon.icns` for macOS
- `icon.ico` for Windows
- `icon.png` for Linux

`electron-builder` can use these names automatically when they are present in `electron/build/`.
